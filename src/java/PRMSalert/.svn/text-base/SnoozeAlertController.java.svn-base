/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package PRMSalert;
import alertsystemdatabase.*;
import java.sql.Timestamp;
import java.util.*;
/**
 *
 * @author Yogeswar
 */
public class SnoozeAlertController {
    
    private Alert alert;
    private DBCommandInvoker dbCommandInvoker;
    
    
    SnoozeAlertController()
    {
        //this.alertInfo = alertInfo;
    }
    
    public void snooze(int snoozeTime)
    {
    /*   Date snooze = this.alertInfo.getAlertTime();
       snooze.setMinutes(snooze.getMinutes() + snoozeTime);
       this.alertInfo.setAlertTime(snooze);
       updateAlertInfo = new DBUpdateAlertInfoCommand(alertInfo);
       dbCommandInvoker = new DBCommandInvoker(updateAlertInfo);
       dbCommandInvoker.execute();        */
    }
    }
